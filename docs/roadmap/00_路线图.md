# TS_RS 路线图

## 当前目标

```text
先把 TS_RS 从 UI 方案推进到可开发架构。
再按 todo.csv 小步进入实现。
```

长期 goal 执行入口：

```text
docs/roadmap/01_长期Goal执行目标.md
```

## 阶段划分

### M0：路线与架构冻结

状态：进行中

目标：

```text
确认 STEP-only + RebarSmart + Detail 路线。
确认 UI 顶层页签、Dock、Viewer 交互。
确认系统模块边界。
确认 todo.csv 任务队列。
```

验收：

```text
docs/ui 完整。
docs/adr 完整。
docs/architecture 完整。
docs/roadmap 完整。
todo.csv 有 next 任务。
```

### M1：RebarSmart 证据与参数模型

状态：进行中

目标：

```text
建立 RebarSmart 资源、PDF、CATNls、INI、IDA 地址索引。
建立 GenerateRebarData 参数结构。
建立第一批钢筋创建命令的 Dock 字段证据清单。
```

说明：

```text
这是 M1 证据收集阶段的历史目标。
2026-06-16 TODO-016 后，P0A 验收已收缩为一个最小闭环，
不再要求 8 个命令全部进入 P0A。
```

首批对象：

```text
定距钢筋
定数钢筋
角筋
拉结筋
插筋
柱锥环盘面筋
箍筋
自定义钢筋
```

验收：

```text
每个 P0 生成器有证据入口。
定距 / 定数字段有专项文档。
RebarGrade / RebarDiameter / RebarWeight / GenerateRebar*.ini 字段入口清楚。
```

当前已完成：

```text
TODO-001：RebarSmart 证据索引。
TODO-002：GenerateRebarData 字段地图。
TODO-003：定数钢筋专项证据文档。
TODO-004：定距钢筋证据补全审计。
```

TODO-004 已确认：

```text
定距钢筋文档继续作为 FixDistancePanel / FixDistanceCenterlineGenerator 主证据入口。
Default/GenerateRebarFixDistance.ini 为 57 字段。
RebarCreation.dll 只作为命令和 UI 层证据。
AfrRebarObjImp.dll / AfrRebarCore3D.dll 是后续纯算法拆解主证据。
辅助布筋区间 Ratio=1.0 属于必须继续确认的 GAP。
```

### M2：纯算法层

状态：已进入

目标：

```text
先不接 OCCT，只做可单测的 RebarSmart 纯算法。
```

范围：

```text
SpaceListParser
CalSectionCount
PriorityCountDistributor
PrioritySpaceDistributor
PriorityListDistributor
GuideCurveZoneCalculator
GuideSurfaceOffsetCalculator
材料 / 等级 / 直径表加载
```

验收：

```text
单元测试覆盖正常输入、边界输入、异常输入。
算法文档记录证据函数或资源来源。
```

当前已完成：

```text
TODO-005：纯算法开发计划。
TODO-006：初始化 app 工程骨架。
TODO-006A：旧实现项目 Qt / OCCT 可复用审计。
TODO-007：SpaceListParser。
TODO-008：PriorityCountDistributor / PrioritySpaceDistributor。
TODO-009：PriorityListDistributor。

产物：
  docs/architecture/01_纯算法层实现计划.md
  docs/architecture/02_TODO-006_app工程骨架实现记录.md
  docs/architecture/03_旧实现项目QtOCCT可复用审计.md
  docs/architecture/04_TODO-007_SpaceListParser实现记录.md
  docs/architecture/05_TODO-008_PriorityDistributors实现记录.md
  docs/architecture/06_TODO-009_PriorityListDistributor实现记录.md

结论：
  旧实现项目可以作为 Qt / OCCT / STEP / Viewer / 选择 / IO 零件库。
  旧实现项目不能作为 TS_RS 钢筋生成业务来源。
  TODO-007 SpaceListParser 已实现 SL-001..SL-015。
  TODO-008 PriorityCount / PrioritySpace 已实现 PC-001..PC-009 / PS-001..PS-010。
  TODO-009 PriorityList 已实现 PL-001..PL-010。
  TODO-010 GuideSurfaceOffset / GuideCurveZone 已实现 GO-001..GO-012 / GZ-001..GZ-011。
  TODO-011 IGeometryEngine 接口设计已实现 GEOM 契约测试。
  TODO-012 OcctGeometryEngine P0 已实现 OCCT 直线/折线几何后端。
```

### M3：GeometryEngine + FixDistance / FixNumber

目标：

```text
接入 OCCT 几何能力，但 RebarSmart 生成器只依赖 IGeometryEngine。
```

范围：

```text
IGeometryEngine
OcctGeometryEngine
curveLength
pointAtLength
tangentAtLength
projectWire
offsetWire
assemble
sweepCircle
FixDistanceCenterlineGenerator
FixNumberCenterlineGenerator
```

验收：

```text
简单 STEP 上能选面 / 边。
生成定距 / 定数钢筋中心线。
可选生成实体钢筋。
RebarModel 可保存生成结果。
```

### M4：Qt6 / OCCT 操作闭环

目标：

```text
用户能在 UI 中打开 STEP、选择几何、填参数、预览、应用、保存。
```

范围：

```text
主窗口
Viewer
左侧工程树 / 钢筋树
右侧 Dock 框架
FixDistancePanel 或 FixNumberPanel
.tsrebar 保存 / 回读
```

验收：

```text
打开 STEP。
选边 / 面。
生成一组钢筋。
保存工程。
关闭重开恢复。
```

### M5：Detail 包兼容导出

目标：

```text
新系统生成的 RebarModel / DrawingModel 能导出老图石 CAD 插件可读 Detail 包。
```

范围：

```text
DetailPackageReader
DetailPackageWriter
DetailPackageExporter
Detail.xml
DetailNN.stl XML
StbGroup / Std / StbGeo / StbTable / MaterialTable 映射
```

验收：

```text
旧 Detail 包可读。
旧 Detail 包 round-trip 可写。
极简 Detail 包可生成。
人工用 CAD 插件 autoin 验证导入。
```

### M6：扩展筋型与工程图质量

目标：

```text
补齐 RebarSmart 更多生成器和工程图能力。
```

范围：

```text
Polyline
Stirrup
Dowel
Tie
Sweep
Helix
Corner
Frustum
Customize
基础标注
下料表
PDF / SVG / Excel / CSV
```

验收：

```text
真实工程样本逐步建立 golden。
常用生产流程可试用。
```

## 时间口径

```text
3-4 个月：演示闭环 MVP。
6-9 个月：内部试用版。
9-15 个月：生产核心替代。
15-24 个月：高置信度替代。
24+ 个月：极端边界和复杂出图持续补齐。
```

## 当前 next

```text
TODO-024：极简 Detail 包生成 + autoin 验证。
状态：blocked，等待旧 AutoCAD 插件 autoin 人工验证。

生成最小 Detail.xml + Detail01.stl XML，
并由旧 AutoCAD 插件 autoin 人工验证。

TODO-022 已完成：
  DetailPackageReader P0。
  使用真实 todo66 fixture manifest 校验 hash。
  读取 Detail.xml + Detail01..04.stl。
  保留 rawXml / source file / sheetIndex。
  输出 knownSummary 统计。
  不写 Detail 包，不映射 DrawingModel / RebarModel。

TODO-023 已完成：
  DetailPackageWriter preserve-mode round-trip。
  使用 rawXml passthrough 写出旧包。
  写出后 Reader 能再读。
  文件数量、knownSummary、未知节点 / 属性 P0 统计不减少。
  不做 minimal generate，不做 autoin，不接 DrawingModel / RebarModel。

TODO-024 代码侧已完成：
  DetailPackageWriter::writeMinimalSectionLinePackage。
  生成 Detail.xml + Detail01.stl。
  Reader 可重新读回 1 个 ViewPort、1 个 PartDetailDrawing、1 条 section-line。
  已提交 GC-004 固定人工验证包：
    docs/validation/golden_cases/GC-004/minimal_detail_package/
  旧 AutoCAD 插件 autoin 尚未人工验证，因此不能宣称 Detail 兼容完成。

TODO-020 已完成：
  STEP 导入与水泥灰显示。
  GC-001 真实 123.stp 导入统计和截图已记录。
  xhigh review 无 Critical / Important。

TODO-020B 已完成：
  ShapeStore + TopologyBindingRegistry P0。
  GC-002 自动 box case 已记录。
  真实工程 STEP collision 仍待扩展。

已冻结：
  老图石 Ribbon 外壳。
  P0A 一个最小闭环。
  Detail round-trip 前置。
  TopologyBinding 前置。
  RebarSmart 单位 / 枚举 / 默认值前置。

P0A 门禁：
  docs/roadmap/03_P0A最小闭环与前置验证门禁.md

执行入口：
  docs/roadmap/01_长期Goal执行目标.md
  todo.csv
```

2026-06-17 外部严格评审后，近期代码顺序调整为：

```text
TODO-020   STEP 导入与水泥灰显示
TODO-020B  ShapeStore + TopologyBindingRegistry P0
TODO-020C  外部评审条件收口
TODO-022   DetailPackageReader P0
TODO-023   DetailPackageWriter round-trip
TODO-024   极简 Detail 包生成 + autoin 验证
TODO-020D  TopologyBindingRegistry P1 hardening
TODO-020E  StepSession / ImportedModelStore 主链路
TODO-020G  CommandService skeleton guardrails
TODO-021   Viewer 选择系统
```

并行门禁：

```text
TODO-020F  STEP import unit and scale policy
```

说明：

```text
TODO-021 必须在 TODO-020B 之后。
Viewer 选择系统后续必须输出 stable binding id，
不能把裸 AIS / TopoDS 直接交给 generator。

Detail Reader / Writer / autoin 前置，
是为了尽早验证老图石 CAD 插件兼容出口。

TODO-020C 是 2026-06-17 外部评审后的条件收口节点，
证明 README / todo / Detail fixture manifest / 020B 边界已经统一。

TODO-020D / TODO-020E / TODO-020G 必须在 TODO-021 前完成，
否则 Viewer 选择系统容易绕过 stable binding 或 CommandService。

TODO-020F 不阻塞 TODO-021。
它必须在 RebarModel / Schedule / generator->Detail 链路前完成，
避免 STEP 几何单位、RebarSmart 内部单位和 Detail 输出单位混用。
```
